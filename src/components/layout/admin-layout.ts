/** Shared max width for all admin shell chrome (header, nav, main). */
export const ADMIN_SHELL_MAX_WIDTH_CLASS = 'max-w-6xl'

/** Readable inner width for form / FAQ / prose-heavy admin pages. */
export const ADMIN_NARROW_CONTENT_CLASS = 'max-w-2xl'

/** Slightly wider inner width for denser forms and managers. */
export const ADMIN_COMFORTABLE_CONTENT_CLASS = 'max-w-3xl'
